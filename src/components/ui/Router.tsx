import { BrowserRouter} from 'react-router-dom';
import React from "react";
import { RoutAnimation } from './RoutAnimation';

export const Router = () => {
  
  return (
    <BrowserRouter>
      <RoutAnimation />
    </BrowserRouter>
  );
};

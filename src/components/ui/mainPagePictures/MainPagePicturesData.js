import CloudTL from '../../../icons/CloudTL.svg';
import CloudTR from '../../../icons/CloudTR.svg';

const MainPicturesData = [
  {id: 1, wrapperClass: `cloudTL`, src: `${CloudTL}`, alt: `CloudTL`},
  {id: 2, wrapperClass: `cloudTR`, src: `${CloudTR}`, alt: `CloudTR`},
];

export {MainPicturesData};
import { BrowserRouter} from 'react-router-dom';
import { RoutAnimation } from './RoutAnimation';

export const Router = () => {
  
  return (
    <BrowserRouter>
      <RoutAnimation />
    </BrowserRouter>
  );
};
